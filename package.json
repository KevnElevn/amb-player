{
  "name": "ambience-gen",
  "version": "1.0.0",
  "description": "Web app to arrange sounds to create ambient pieces",
  "main": "index.js",
  "scripts": {
    "test": "mocha server/tests/api-tests.js",
    "start": "npm run build && node server/index.js",
    "build": "cd client && npm install --production && npm run build"
  },
  "engines": {
    "node": "16.14.0"
  },
  "author": "Kevin Li",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "cors": "^2.8.5",
    "express": "^4.18.1",
    "express-oauth2-jwt-bearer": "^1.1.0",
    "pg-promise": "^10.11.1"
  },
  "devDependencies": {
    "chai": "^4.3.6",
    "chai-http": "^4.3.0",
    "dotenv": "^16.0.1",
    "mocha": "^10.0.0"
  }
}
